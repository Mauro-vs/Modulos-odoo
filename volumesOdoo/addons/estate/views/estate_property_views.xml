<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Acción principal -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_estate_property_list"/>
        <field name="search_view_id" ref="view_estate_property_search"/>
    </record>

    <!-- Vista de búsqueda -->
    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Properties">
                <field name="name"/>
                <field name="cp"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="facades"/>
                <filter name="postcode_group" context="{'group_by': 'cp'}" string="Group by Postcode"/>
            </search>
        </field>
    </record>

    <record id="view_estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <sheet>
                    <!-- Título grande con el nombre -->
                    <div>
                        <label for="name"/>
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>

                    <!-- Datos principales en dos columnas -->
                    <group>
                        <group>
                            <field name="property_type_id"/>
                            <field name="cp"/>
                            <field name="date_availability"/>
                            <field name="tag_ids" widget="many2many_tags"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_offer"/>
                            <field name="selling_price" readonly="1"/>
                        </group>
                    </group>

                    <!-- Pestañas -->
                    <notebook>
                        <!-- Descripción -->
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area"/>
                                <field name="garden_orientation"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <!-- Otra información -->
                        <page string="Other Information">
                            <group>
                                <field name="buyer_id"/>
                                <field name="salesman_id"/>
                            </group>
                        </page>
                        <!-- Ofertas -->
                        <page string="Offers">
                            <field name="offer_ids"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>


    <record id="view_estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties">
                <field name="name" width="25%"/>
                <field name="cp" width="10%"/>
                <field name="bedrooms" width="8%"/>
                <field name="living_area" width="10%"/>
                <field name="expected_price" width="12%"/>
                <field name="selling_price" width="12%"/>
                <field name="date_availability" width="13%"/>
            </list>
        </field>
    </record>
</odoo>
